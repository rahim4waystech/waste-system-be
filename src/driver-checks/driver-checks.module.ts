

/**
 * This file was generated by Four Ways Technology
 *
 * On Fri Aug 07 2020 21:32:36 GMT+0100 (British Summer Time)
 */
import { Module, MiddlewareConsumer } from '@nestjs/common';
import { DriverChecks } from './driver-checks.entity';
import { TypeOrmModule } from '@nestjs/typeorm';
import { DriverChecksService } from './driver-checks.service';
import { DriverChecksController } from './driver-checks.controller';
import { AuthModule } from 'src/auth/auth.module';
import { AuthMiddleware } from 'src/auth/auth.middleware';

@Module({
    imports: [TypeOrmModule.forFeature([DriverChecks]), AuthModule],
    providers: [DriverChecksService],
    exports: [],
    controllers: [DriverChecksController],
  })
  export class DriverChecksModule {
    configure(consumer: MiddlewareConsumer) {
      consumer
      .apply(AuthMiddleware)
      .forRoutes('driver-checks');
    }
  }
